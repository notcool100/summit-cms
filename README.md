# summit-cms

Eight-page marketing site build (home, about, capabilities, careers, contact, industries, projects, project detail) currently implemented as static HTML/CSS/JS pages for review.

## Current structure

- `home.dc.html`, `about.dc.html`, `capabilities.dc.html`, `careers.dc.html`, `contact.dc.html`, `industries.dc.html`, `projects.dc.html`, `project-phoenix.dc.html` — page templates
- `SiteHeader.dc.html`, `SiteFooter.dc.html` — shared header/footer partials
- `site.js`, `support.js`, `image-slot.js` — supporting scripts

## Roadmap

Planned future stack:

- **Backend:** .NET
- **Frontend:** Next.js (TypeScript)
